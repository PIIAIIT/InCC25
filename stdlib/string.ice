{
# Für die ganzen Stringfunktionen, die die Sprache haben soll. #
# LEN #
länge := lambda liste -> länge(liste) .;

# SUBSTRING #
substring := lambda (string, start, end) -> {
    # TODO #
    end - start
}.;

# SPLIT #
split := lambda (string, delim) -> {
    # TODO #
}.;

# UPPER #
upper := lambda (string) -> {
    # TODO #
}.;

# LOWER #
lower := lambda (string) -> {
    # TODO #
}.;

}
