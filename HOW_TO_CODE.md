# Sprachspezifikation: Ausdrucksparser

Diese Datei wurde aus der `parser.out`-Datei von PLY generiert.
Die folgenden Regeln beschreiben die Grammatik der Sprache in einer BNF-ähnlichen Notation.

## Startsymbol
- `S'`

## Produktionen
`S'` ::= expression   \\
`expression` ::= NUMBER   \\
`expression` ::= FLOAT   \\
`expression` ::= STRING   \\
`expression` ::= IDENTIFIER   \\
`expression` ::= LPAREN expression RPAREN   \\
`expression` ::= expression PLUS expression   \\
`expression` ::= expression MINUS expression   \\
`expression` ::= expression TIMES expression   \\
`expression` ::= expression DIVIDE expression   \\
`expression` ::= expression DIVIDE_CEIL expression   \\
`expression` ::= expression DIVIDE_FLOOR expression   \\
`expression` ::= expression MOD expression   \\
`expression` ::= expression EXP expression   \\
`expression` ::= expression AND expression   \\
`expression` ::= expression OR expression   \\
`expression` ::= expression XOR expression   \\
`expression` ::= expression POWER expression   \\
`expression` ::= NOT expression   \\
`expression` ::= MINUS expression   \\
`expression` ::= PLUS expression   \\
`expression` ::= expression IMAG   \\
`comparison` ::= expression comparison_op expression   \\
`comparison` ::= comparison comparison_op expression   \\
`expression` ::= comparison   \\
`comparison_op` ::= GREATER_THAN   \\
`comparison_op` ::= SMALLER_THAN   \\
`comparison_op` ::= UNEQUALS   \\
`comparison_op` ::= EQUALS   \\
`comparison_op` ::= SMALLER_EQUALS   \\
`comparison_op` ::= GREATER_EQUALS   \\
`assign_expression` ::= IDENTIFIER ASSIGN expression   \\
`expression` ::= assign_expression   \\
`expression` ::= IDENTIFIER PLUS_ASSIGN expression   \\
`expression` ::= IDENTIFIER MINUS_ASSIGN expression   \\
`expression` ::= IDENTIFIER TIMES_ASSIGN expression   \\
`expression` ::= IDENTIFIER POWER_ASSIGN expression   \\
`expression` ::= IDENTIFIER DIVIDE_ASSIGN expression   \\
`expression` ::= IDENTIFIER DIVIDE_FLOOR_ASSIGN expression   \\
`expression` ::= IDENTIFIER DIVIDE_CEIL_ASSIGN expression   \\
`expression` ::= IDENTIFIER GREATER_THAN_ASSIGN expression   \\
`expression` ::= IDENTIFIER SMALLER_THAN_ASSIGN expression   \\
`expression` ::= IDENTIFIER GREATER_EQUALS_ASSIGN expression   \\
`expression` ::= IDENTIFIER SMALLER_EQUALS_ASSIGN expression   \\
`expression` ::= IDENTIFIER EQUALS_ASSIGN expression   \\
`expression` ::= IDENTIFIER UNEQUALS_ASSIGN expression   \\
`expression` ::= IDENTIFIER AND_ASSIGN expression   \\
`expression` ::= IDENTIFIER OR_ASSIGN expression   \\
`expression` ::= IDENTIFIER XOR_ASSIGN expression   \\
`expression` ::= IDENTIFIER EXP_ASSIGN expression   \\
`expression` ::= IDENTIFIER MOD_ASSIGN expression   \\
`expression` ::= BEGIN statements END   \\
`expression` ::= BEGIN statements SEMICOLON END   \\
`expression` ::= BEGIN END   \\
`statements` ::= expression   \\
`statements` ::= statements SEMICOLON expression   \\
`expression` ::= IF expression THEN COMMA expression DOT   \\
`expression` ::= IF expression THEN COMMA expression else_elif_body DOT   \\
`else_elif_body` ::= COMMA ELIF IF expression THEN COMMA expression else_elif_body   \\
`else_elif_body` ::= ELSE expression   \\
`else_elif_body` ::= COMMA ELIF IF expression THEN COMMA expression   \\
`expression` ::= WHILE expression THEN COMMA expression DOT   \\
`expression` ::= LOOP IDENTIFIER IN expression LOOPTHEN expression DOT   \\
`expression` ::= OPEN_BRACKETS expression ITER expression CLOSED_BRACKETS   \\
`expression` ::= CLOSED_BRACKETS expression ITER expression CLOSED_BRACKETS   \\
`expression` ::= OPEN_BRACKETS expression ITER expression OPEN_BRACKETS   \\
`expression` ::= CLOSED_BRACKETS expression ITER expression OPEN_BRACKETS   \\
`expression` ::= LAMBDA parameter LAMBDA_ARROW expression   \\
`parameter` ::= LPAREN parameter_pos RPAREN   \\
`parameter` ::= IDENTIFIER   \\
`parameter` ::= empty   \\
`parameter_pos` ::= parameter_pos_list   \\
`parameter_pos_list` ::= IDENTIFIER COMMA parameter_pos_list   \\
`parameter_pos_list` ::= IDENTIFIER   \\
`parameter_pos_list` ::= parameter_keywords   \\
`parameter_keywords` ::= parameter_kw_list   \\
`parameter_kw_list` ::= IDENTIFIER COLON expression COMMA parameter_kw_list   \\
`parameter_kw_list` ::= IDENTIFIER COLON expression   \\
`parameter_kw_list` ::= parameter_infty   \\
`parameter_infty` ::= IDENTIFIER DOTS   \\
`parameter_expr` ::= parameter_pos_expr   \\
`parameter_expr` ::= empty   \\
`parameter_pos_expr` ::= expression COMMA parameter_pos_expr   \\
`parameter_pos_expr` ::= expression   \\
`parameter_pos_expr` ::= parameter_keywords_expr   \\
`parameter_keywords_expr` ::= expression COLON expression COMMA parameter_keywords_expr   \\
`parameter_keywords_expr` ::= expression COLON expression   \\
`empty` ::= <empty>   \\
`expression` ::= expression LPAREN parameter_expr RPAREN   \\
`let_assign` ::= IDENTIFIER EQUALS expression COMMA let_assign   \\
`let_assign` ::= IDENTIFIER EQUALS expression   \\
`expression` ::= LET let_assign IN expression DOT   \\
`param_list` ::= expression COMMA param_list   \\
`param_list` ::= expression COMMA expression   \\
`expression` ::= expression CONS expression   \\
`expression` ::= LPAREN param_list RPAREN   \\
`expression` ::= expression OPEN_BRACKETS PLUS CLOSED_BRACKETS   \\
`expression` ::= expression OPEN_BRACKETS expression CLOSED_BRACKETS   \\
`expression` ::= NULL   \\
`expression` ::= OPEN_BRACKETS param_list CLOSED_BRACKETS   \\
`expression` ::= OPEN_BRACKETS expression CLOSED_BRACKETS   \\
`expression` ::= OPEN_BRACKETS CLOSED_BRACKETS   \\
`assignment_list` ::= assign_expression SEMICOLON assignment_list   \\
`assignment_list` ::= assign_expression SEMICOLON assign_expression   \\
`expression` ::= STRUCT BEGIN assignment_list END   \\
`expression` ::= STRUCT BEGIN assign_expression END   \\
`expression` ::= STRUCT BEGIN END   \\
`expression` ::= expression LAMBDA_ARROW expression   \\
`file` ::= STRING   \\
`expression` ::= IMPORT file   \\
`expression` ::= MATCH expression WITH cases DOT   \\
`cases` ::= CASE expression COLON expression DOT cases   \\
`cases` ::= CASE expression COLON expression DOT   \\
